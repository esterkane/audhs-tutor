{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Copy to .claude/settings.local.json (gitignored). Personal overrides only.",
  "model": "opus",
  "permissions": {
    "allow": [
      "Bash(open http://localhost:*)"
    ]
  },
  "env": {
    "ANTHROPIC_MODEL": ""
  }
}

# Creative source selection

Continue creative-material learning without two attachments the owner explicitly chose to skip.
Select topic-relevant prose and actual programming examples; ignore project asset reports and orientation passages.
Reuse existing area/draft APIs and evidence validation. No schema, event, preference, mode, energy or UI changes.
No new accessibility interaction. Existing drafts and active lessons remain untouched; new content stays unpublished.

Acceptance: token boundaries avoid Canva/canvas and character/CharacterTextSplitter collisions; legacy stem terms remain valid; trusted CSS examples can be selected; asset reports and intros are excluded; existing role/trust/latest/dedup checks stay intact.

Reproduction: two failing regressions in test_area_selection.py before patch. Verification: 377 backend tests, 63 frontend tests and repository lint/type checks pass. Required code review fixed technical plural regression; re-review has no blockers/majors. Pedagogy review fixed exact-answer worked examples and generic rubrics in private starter content; final six two-lesson drafts have no blockers/majors. No API contract or UI change, so type generation and browser journeys were not changed.
Limit: lexical matching is still heuristic; broad semantic ambiguity requires source review. Code support is bounded to explicit CSS/JavaScript/Python/SQL terms and complete fenced examples in the excerpt.

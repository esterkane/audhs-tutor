# Local audio file UX and waveform

Owner cannot tell what choosing a local file or Start analysis does. Research official Audacity import/transport, Sonic Visualiser waveform/time ruler and WaveSurfer docs. Use their separation of load, playback and visual representation, without copying complete editing suites.
Add pinned WaveSurfer visual-only local waveform (up to 20 MiB, low sample-rate overview), never autoplay/independent playback; preview failure must not block playback. Explicit Play file silently/with sound, file name, elapsed time, no-file disabled state, completed state, Stop/restart explanation. Motion-reduced mode explicitly uses Capture current picture. No upload/transcription/AI; analysis means current sound levels only.
Single playback source remains existing Web Audio adapter. Waveform destroyed on file change/unmount; ignore late preview results. No schema/mastery/learner events changes. Progress numbers outside live region; reduced-motion waveform static. Existing sensory preference respected.
Acceptance: no autoplay on selection, mode-specific action names, no hidden data upload, resource cleanup, waveform-failure fallback, real browser waveform+play/capture/stop and tests of retained final position.

Verification: 86 full frontend tests passed; final 8 focused tests pass (waveform prepare/mute/reduced motion/cleanup/failure/size fallback, audio final position, completion→demo reset). Isolated Chromium waveform-ready/no-autoplay/play/capture/stop journey passes. make lint and final frontend lint/types/production build pass; large-chunk advisory remains. Screenshot inspected. Code/pedagogy reviews cleared blockers/majors; minor status fixes applied. Initial stale Vite dependency cache caused React identity failure, resolved by removing only generated cache and rerunning browser test.

# Visualizer UX clarity

Owner reports confusing UX. Reproduction: default screen exposes raw JSON, Apply below advanced editors, duplicate example-loading entry and a default-example guide alongside custom edits. A user must scroll through implementation details to preview basic changes.
Simplify default path: silent demo → change picture → Apply; adjacent plain-language draft/apply/save status, Save/Export disabled while draft differs to prevent silently preserving older settings. Collapse JSON and the example-specific guide; retain optional block tools and explicit controls. Keep contextual explanation sticky on desktop.
No schema, model, learner state or evaluation changes. Existing sensory settings and low-energy path retained. Keyboard-native disclosure controls. Verify first-use visibility, pending/save behavior, accessibility and existing browser journey.

Verified: 84 frontend tests, isolated Chromium journey and make lint pass. Default browser screenshot reviewed. Final wording fixes checked with five affected route tests and frontend lint/types. Code/pedagogy reviews cleared blockers/majors; fixed custom-preset low-energy copy, invalid-draft guidance and Apply-reset explanation. No claim of app-wide UX review or user-tested usability.

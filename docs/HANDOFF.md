## Session guidance — 2026-09-24

Simplified Session/Review/Recap guidance: current activity and explicit next action; optional plan/settings, alternate explanations, source descriptions and score details collapsed; continuing the plan is primary after feedback, another question optional. Stop/recap stays visible, with honest unchecked-text persistence wording. Experiment status remains visible. Timer prompt follows the activity instead of preceding it.

Verification: 339 backend, 53 frontend, lint, axe and all 7 isolated sandbox browser journeys passed; code/pedagogy review findings addressed. See `docs/slices/session-guidance.md`. No live learning attempts or curriculum activation performed. Follow-up: broader Home/navigation clarity and persistence of unchecked answers on leaving; this slice does not claim those are resolved.

## Assessment relevance — 2026-09-23

Removed automatic source-cut clozes from new curriculum proposals. Curriculum prompt v2 requests source-supported applications and reasoning; conservative logistics filters reject unsuitable questions/criteria, and missing required assessment dimensions block activation. See `docs/slices/assessment-relevance.md` for scope, tests and evaluation limitations.

A reviewed correction is saved as an unpublished local draft. Active questions and learning history are unchanged. Pending owner choice: replace existing active questions or leave the correction for review. Do not simply publish expecting replacement: current publication adds assessments. Replacement must retire old question eligibility and review cards while preserving historical answers/evidence. No retirement implementation is included here.

Verification: 339 backend / 51 frontend tests and lint passed; code/pedagogy reviews cleared. Local draft-model evaluation still produced trivia, demonstrating why the validation guard is needed. Broader tutor evaluation passed 3/5 hard checks, with length and premature-solution failures; hosted rubric judging unavailable. These remain explicit follow-up items.

## Lesson activation UI — 2026-09-23

The material-to-lessons screen now explains its purpose and separates course selection, section review and explicit activation. Existing drafts come first; readable goals/practice/assessments and inline source evidence replace default JSON exposure. New draft creation and source-role settings are secondary. “Activate lessons” uses the existing publication endpoint and explains that content stays in the local app. Unsaved edits and refreshed draft conflicts are guarded. No live drafts were activated during testing.

Verification: 333 backend tests, 51 frontend tests, lint, axe and read-only browser inspection passed. Code/pedagogy re-reviews cleared; see `docs/slices/lesson-activation-ui.md`. Detailed editing remains JSON under Advanced. Continue with the outstanding owner review/activation and evaluation queue below.

## Active ownership — Codex, 2026-09-23

The owner explicitly requested commit, push and full task takeover. Codex owns the remaining engineering/operations queue; Claude can resume later from this file. Do not edit concurrently.

Verified implementation commits: `63e3a7b` drafting evidence, `7153f6d` n8n workflow ingestion, and `59fcc2c` takeover handoff are pushed. Lint, 319 backend and 42 frontend tests passed; all four GitHub CI jobs passed for `59fcc2c`. Historical uncommitted/unpushed notes below describe earlier snapshots.

Follow-up completed: source-grounded review of existing unpublished drafts corrected unsupported objectives, assessment citations, rubric criteria and prerequisite gates. A private rehearsal passed without changing live learner history. Resource warnings were reconciled against saved artifacts and ordinary browser behavior; selected public source files were imported with provenance. Browser-blocked resources remain explicitly unresolved. Detailed operational evidence stays outside Git. No application code changed in this follow-up.

Grading safeguards completed: substantive free-text answers now require semantic review; keyword overlap cannot bypass it. Structured grades require each rubric criterion exactly once with nonempty evidence. Unusable grades return a retryable error before learning-state writes; assessment/challenge/listening input remains available for retry. See `docs/slices/grading-evidence.md`. Final lint, 333 backend and 44 frontend tests passed; code and pedagogy reviews addressed. No live history changed. This closes the keyword-bypass engineering item, not model accuracy or learning-outcome evaluation.

Remaining work:
1. Owner review and explicit publication approval for course drafts; nothing was published to the live curriculum.
2. Owner resolution of browser-blocked downloads; preserve the block rather than bypassing it.
3. Evaluate semantic grading accuracy and learning outcomes across lessons. The private rehearsal and grading safeguards are not a full pedagogical validation.
4. Add other JSON readers only when instructional value and safe extraction rules justify them; general JSON remains unsupported.
5. Personal voice benchmark remains dependent on owner recordings; do not close the gate using substitute voices.

Claude can continue from this queue. Source-review and resource-recovery work above supersedes the corresponding pending items in historical entries below.

## Continued implementation — Codex, 2026-09-23

Built-in n8n workflow ingestion is implemented and live-tested. New `knowledge/ingest/workflows.py` parses recognizable n8n exports, including archive members, into bounded node/connection/teaching-note evidence. Integrated through `loaders.py`; credentials, parameter values, embedded scripts and runtime data remain omitted. General JSON remains unsupported. No dependencies/migrations/UI changes. README, ROADMAP, COURSE-MATERIAL-STATUS and slice updated. See `docs/slices/n8n-workflows.md`.

Final `UV_NO_SYNC=1 make lint` and `UV_NO_SYNC=1 make test`: 319 backend + 42 frontend passed. Code review: no blockers/majors; both minor findings addressed with classification/bounds tests (13 workflow tests). Live existing-course import: 239 document versions, 1414 unique indexed chunks, 3108 deduped; 117 unchanged, 1 reference-only, six unsupported other JSON formats, two gated images; zero parser/access/retryable errors. Some versions replace the earlier one-off structure imports; not all 239 are new documents. Real filtered retrieval passed. SQLite quick check/FKs passed, learner profile/33 events/7 sessions unchanged.

Table QA: generated on a private snapshot with local models only, then manually checked against original notebook and both stored passages. Merged only the new lesson into the live Question Answering draft (now four skills), preserving its three existing lessons. Replaced model-default MCQ with a source-backed task-string question, added page-count assumption exercise, aligned explain-back rubric. Pedagogy review: no blocker/major; removed unnecessary fine-tuning prerequisite per minor finding. Eight open drafts now total 23 skills; none published. Other notebook sections still need scope/rubric review.

Changes remain uncommitted under the owner-invoked commit convention. Keep the prior drafting-evidence group separate from the n8n loader/test/README/ROADMAP/material-status/slice group; HANDOFF documents both. Five inherited commits remain ahead of origin/main. Private scripts, snapshots and course passages remain outside Git.

Next: owner review/publication of selected transformer sections; ordinary-menu handling for the 15 sample scanner button-only resources; selected external-reference fetching; other-format JSON support only if useful. Voice benchmark remains open. No scanner change or new browser session in this follow-up.

## Takeover update — Codex, 2026-09-23

Claude's last session was complete; inherited main is five commits ahead of origin/main through `c452252`. New delta remains uncommitted, respecting the owner-invoked `/commit` convention: `backend/app/orchestrator/drafting.py`, new `backend/tests/test_draft_evidence.py`, new `docs/slices/draft-evidence.md`, and this handoff. Suggested commit: `fix(curriculum): preserve selected drafting evidence and citations`. No push this turn.

Reproduced and fixed first-passage truncation erasing later model evidence, and selected passages beyond chunk eight missing from lesson citations. Code/pedagogy review: no blockers/majors; fallback regression added. Final backend 306 tests; frontend 42; lint (ruff, mypy, tsc, eslint) green. Private local-model snapshot check returned all eight architecture lessons with resolvable citations; no hosted calls. See slice for limits.

Live curation: reviewed the narrative `text` training-data source, marked both copies supplemental, removed its spurious lesson/assessments from the architecture draft. Eight open transformer drafts now contain 22 skills, none published. Learner profile, all 33 learning events and seven sessions matched the pre-operation snapshot. Still review Input Embedding's conceptual rubric, notebook-only coverage and MCQ answer keys. Do not publish automatically.

Authorized three-course browser scan+download sample completed:
- Transformer: 8 sections / 49 lectures / 32 resources (26 attachments + 6 external links), zero issues/unfinished, clean complete. Recovered Table QA notebook imported/indexed: one new document, two chunks. Question Answering draft must be reviewed/extended separately.
- LangChain: 29 sections / 197 lectures / 192 external links; six precisely located button-only issues, zero unfinished. Previously no curriculum detected; now full curriculum traversed. External links are references, not downloaded content.
- n8n: 22 sections / 233 lectures / 303 resources (246 attachments + 57 external links); nine precisely located button-only issues, zero unfinished. Eight new PDF documents imported/indexed, 21 chunks; 109 unchanged plus one reference-only. 245 workflow JSON entries remain unsupported (including archive members); two images gated by this no-media run. No parser, access or retryable errors.

Manifests under the existing Udemy Resources course directories carry exact remaining lecture/button labels. No button-only action or browser block was bypassed. This verifies the sample, not a clean 39-course rescan or all external URLs. Downloader source was not changed during this takeover; live results added to its CHANGELOG.

Next: owner-reviewed course publication; bounded n8n workflow-JSON ingestion design (treat workflows as source data, never execute them); review/fetch selected new external references with existing provenance rules; ordinary-menu handling for the 15 button-only resources. Personal voice benchmark still open. Private snapshots/reports/scripts live under the Codex task work/takeover directory, never public Git.

## Latest operations — Codex, 2026-09-21

The four approved slices are committed locally (not pushed): `8167925` browser journeys, `5a78dd4` CodeMirror editor, `7bd70ac` encrypted backups, `6ec89df` local pinned Pyodide. Shared Makefile/CI/editor changes were split in the index; inherited working files were preserved. Fresh validation of the combined tree: `UV_NO_SYNC=1 make lint` passed; `UV_NO_SYNC=1 make test` passed (backend 256, frontend 42); `UV_NO_SYNC=1 make test-e2e` passed all 7 journeys; `UV_NO_SYNC=1 make pyodide-verify` passed. `UV_NO_SYNC=1` preserves the installed optional STT environment. Remote CI verified on 2026-09-21: run 35620554697 on `cc5aee3` (main) passed all four jobs — frontend, browser journeys (Playwright on the sandbox stack, 1m37s), migrations, backend — after the owner's `169d819` (scipy in CI test deps; media test no longer assumes a local audio decoder).

The backend-restart note below is historical: on rechecking, port 8000 answered corpus statistics and voice readiness, with Whisper and Kokoro ready at the configured local URL. No process was stopped or restarted. Voice remains inactive; VAD uses the energy fallback while Silero is absent. The personal Stage-5 benchmark gate remains open.

Course material processing: the complete 39-course browser batch finished (25 clean scans, 14 with recorded resource/menu gaps). All 1,163 current direct attachments passed size checks; all 297 ZIP attachments passed CRC checks. External downloads: 104 successful source records, 19,621 files, all checksum-verified. Text and supported media imports are complete: 21,540 documents, 115,843 stored chunks and 83,538 latest unique searchable chunks, exactly matching Qdrant. All 17 normalized WAV copies were transcribed; 515 image-cache reuses avoided duplicate model work. SQLite integrity and foreign keys passed; original learner/session/event rows are unchanged. Three course-filtered retrieval checks passed. See `docs/COURSE-MATERIAL-STATUS.md` for remaining formats and download gaps. Live STT/TTS verification also passed without using the microphone. Supplemental-resource scope, not all lecture videos; no curriculum drafts have been published by this operation.

Keep `frontend/public/pyodide/` and the private prompt pack out of Git. Separate in-progress voice-benchmark changes (`scripts/bench_voice.py`, ROADMAP/voice-loop notes and `evals/results/bench_voice.json`) are outside these four implementation commits. Do not stage them implicitly.

## Rehearsal of the learning loop on the transformer course (Claude, 2026-09-23, done)

`scripts/rehearse_course.py --course NAME --sessions N` publishes a course's open drafts on a private online-backup snapshot (`data/rehearsal.db`, hosted providers off, budget 0; the live DB is never written), sets `goal.course`, and runs sessions the way the learner meets them — explain + hint turn with the routed local model, citations checked against the course, the assessment rotation graded by the real grader, competency refresh, FSRS, session end; report in `evals/results/rehearse_<course>.json`. Five runs on *LLM Engineering: Understanding & Deploying Transformer models* found and fixed five defects (details `docs/slices/curriculum-drafts.md` 2026-09-23; commits `e7758fe` + the one after it): (1) the first lesson depended on publish order → `skill_node.section`/`order_no` (migration `d4a7b2c9e1f0`, applied to `data/dev.db` by the auto-migrating dev backend) and a goal pick sorted by course order; (2) mastery could never pass 0.42 (no explain-back item for the required 'explanation' dimension) → every drafted skill gets an explain-back item with a rubric from the model's success criteria, all 8 drafts regenerated (23 skills; 8 MCQ, 23 explain-back, 21 cloze; the 8 superseded drafts rejected; the only warnings left are the by-design "check the model MCQ against its source"); (3) sections sorted alphabetically because the resources-export folder names were not parsed → `path_numbers` reads both layouts; (4) the tutor cited other courses' notebooks → retrieval widens skill → course → corpus and records it in the trace (`tests/test_retrieve_scope.py`); (5) the instructor-repo chapter had no folder number → literal label fallback ("chapter 2 …"). **Measured (run 4, before fix 5):** 3 sessions, 0 problems, every cited passage inside the course, mastery 0.51 → 0.61 → next skill unlocked, MCQ/cloze deterministic, explain-back graded by the local LLM (a generic answer scores 0 — correctly; the lecture-based answer passed the rubric level once), 0 hosted calls; turn latency median 16 s (max 21 s) with `gemma3-12b` on `explain`/`hint` — streaming hides most of it, and moving `explain` to `llama31-8b` in Settings › Models is the owner's decision (not changed). Run 5 (after fix 5) confirms the course now opens with chapter 2 › Input Embedding, all citations in-course, 0 problems; its explain-back scored 0 against a rubric asking what an embedding *is* while that notebook only shows how Word2Vec is created — the grader is right, the lesson text is thin (edit the goal/criteria or accept it as a code-along). Gates: lint green, backend 303. **Owner next:** review the 8 regenerated drafts in the Curriculum screen (notes for the notebook-based sections still apply: code-along lessons), publish per section; then the first live session on the course should start with the architecture chapter.

## Course-material stage 4 — scanner gaps closed offline (Claude, 2026-09-21, done; live sample is the owner's)

Work happened directly in `/Users/saru/Downloads/udemy-resource-downloader` (not a git repo; originals copied to `backups/20260921-stage4/` first; its `CHANGELOG.md` has the full list). Starting point was the real 39-course report of 2026-09-21: 14 courses "needs-attention" for 63 × "Menu includes button-only actions" (never saying which lecture), 16 × "Resource menu unavailable" (menus rendering too late) and one course with "No curriculum sections found" (a course-announcement dialog marks the page `aria-hidden`, hiding the curriculum). Changes: scan issues are records with `kind` and the exact section/lecture (+ button labels, dialog heading); non-binding dialogs are closed only through their own close control (`Close`/`Schließen`/`Not now`/`×`, at most three times, recorded under `dismissedDialogs`; a dialog without one ends the scan as partial with `modal-blocking`); a late menu is retried once with a longer wait before `menu-timeout`; button-only menus are recorded with their labels and never clicked; failed downloads carry `reason` (`stopped|timeout|blocked|refused-media|refused-html|http`), `ERR_BLOCKED_BY_CLIENT` = `blocked` and stays a handoff item; `lastScan.complete` is the explicit clean-completion flag (not stopped, no issues, nothing unfetched) and the batch report carries per-course `complete` + `attention`; Terminal `batch --attention` lists `kind: Section › Lecture — resource/buttons` per course without downloading (verified against the real report: old manifests show as `legacy` sentences); public API quota (429, or 403 with `X-RateLimit-Remaining: 0`) is reported with the reset time, recorded as `rate-limited` and retried by the next run, `Retry-After ≤ 30 s` honoured once; GitHub scope unchanged and locked by a test. Offline regression fixtures: closable dialog, blocking dialog, delayed menu, never-opening menu, button-only menu, interrupted download that resumes, browser-blocked fetch (the fixture now declares UTF-8 — without it "Schließen" arrived as mojibake). Tests: 25 Python unit, 10 Node, 17 Playwright fixtures, all green; `doctor`, shell and JS syntax checks pass. README/BATCH_GUIDE/BROWSER_GUIDE updated with the one command (`python3 ~/Downloads/udemy-resource-downloader/udemy_resource_downloader.py batch`) and the honest limits. **Owner next (needs your Chrome):** rerun one course per failure class with the newly copied snippet — LangChain (no sections), LLM Engineering (menu timeouts), n8n (button-only) — then `batch --attention`, then the whole list. Nothing was bypassed: login, CAPTCHAs, browser blocks, paywalls and button-only actions remain yours.

## Course-material stage 3 — curate and draft one course (Claude, 2026-09-21, done)

`docs/slices/course-curation.md`. Delta: `course_source` table + migration `c8e2f1a9b4d0`, `kernel/curriculum.py` (`suggest_role`, `source_roles`, `course_sources`, `set_source_role`, `reset_source_role`; `section_material` uses primary documents only and lists `left_out`; drafts carry `info` notes on what they are built on and what was left out; a section without primary material is refused), routes `GET/PUT/DELETE /api/curriculum/sources`, Curriculum screen › "Sources of <course>" (native role select per document, suggestion reason, "use suggestion"), `PUT /api/curriculum/sources-archive` (one decision per bundle), `scripts/draft_course.py` (dry run on a private, read-only-sourced, self-deleting snapshot by default; `--live` writes drafts and skips sections with an open draft), `tests/test_course_curation.py` (7), `test_curriculum.py` accepts `info`. Both reviews applied (pedagogy F1–F11; code review blocker + majors + minors — see the slice doc): the selection a draft is built on is stored in `payload.selection` and its notes are re-derived on every edit and publish; the model path shares refusal and selection and is tested to never see excluded text. Gates: `make lint` green, backend 291, frontend 42, `make migrate-check` ok. Nothing was published or re-tiered; the live DB was not written (drafts exist only in the discarded snapshot).

**Live drafting (2026-09-21, owner's choice: the transformer course).** "Transformers from Scratch" in the corpus is the 4-document seed sample, so the real material is *LLM Engineering: Understanding & Deploying Transformer models* (43 documents; its "Training Transformer From Scratch" section is the from-scratch part). Roles set through the API per the owner's rule "instructor notes, captions and code first": 16 decisions (the instructor's unnumbered notebooks → primary, four bundled `assets/*_vocab` word-lists → excluded), now 38 primary / 1 supplemental (`external-links`) / 4 excluded. Drafts generated with `use_model` (gemma3:12b, local, $0): the first pass exposed three defects (fixed in `35a2825`: bounded per-call lectures, prose-first excerpts, one skill per lecture); the second pass produced **8 drafts, 0 errors, every citation resolving, one MCQ + 3 criteria + exercises per skill**: (no section) 2 skills · chapter 2 Transformers Architecture 8 · Model Optimization and Deployment 1 (was 3 identical) · Question Answering 3 · Text Classification 2 · Text Generation 4 · Text Summarization 1 · Training Transformer From Scratch 2; the duplicate section "Transformers Architecture" is refused (its three documents duplicate chapter 2). The nine superseded drafts were rejected; nothing was published, no trust tier or learning progress changed. **Review notes for the owner before publishing:** prerequisites are a linear chain in lecture order, not conceptual; the slide-based chapter 2 reads well (masking, autoregression, cross-attention, tokenization trade-offs), while the notebook-based sections stay shallow ("run this cell", dataset loading) because those notebooks are almost entirely code — edit goals/exercises there or accept them as code-along lessons; "Training Transformer From Scratch" has only 2 lecture slots because the same `implement_transformer_layers` notebook is attached to three lectures (duplicates left out) and the transformer-layers content therefore sits under "Getting your data ready"; the model MCQs carry the "no single passage linked" warning by design (check each against the cited passages in the editor). Publish per section from the Curriculum screen.

**Dry run on a snapshot of the live DB (the course choice is still the owner's — two examples):** *Certified Forward Deployed Engineer Mastery* — 100 documents, all members of one project bundle; the first rule marked them all supplemental and refused the draft, so the rule now says: an archive that is a section's **only** material is primary → 30 skills, 862 unique passages, 237 citations (0 unresolved), 30 errors (no assessment per skill: README-style project text yields no cloze sentences), 90 warnings (no criteria/exercises). *LLM Engineering: Understanding & Deploying Transformer models* — 43 documents: 26 primary, 17 suggested supplemental (an unnumbered notebook, link lists); per section 1–8 skills, e.g. "Transformers Architecture" 8 skills, 15 cloze items, 49 citations (0 unresolved), 3 errors, 24 warnings; the (no section) slot has one primary document. **Open pedagogy/coverage questions** recorded in the slice doc: deterministic drafts invent no criteria or exercises (warnings by design); numbered notebooks count as lectures; duplicated lecture text collapses to one slot silently; supplemental sources are not cited as further reading yet. **Owner next:** pick the course, review its Sources panel, propose the drafts (deterministic or with the local model), edit, publish explicitly. `.env.example` now has `VISION_CACHE_DIR` (owner, uncommitted — the commit skill never stages `.env*`; commit it by hand).

**Next: stage 4 — close scanner gaps** in the separate `/Users/saru/Downloads/udemy-resource-downloader` repo (read its README, BATCH_GUIDE, changelog and tests first): bounded handling of delayed resource menus and non-binding course-announcement modals, exact course/section/lecture/resource for button-only actions and failed menus, clean completions vs partial scans, checksummed resume/manifests/originals preserved, no bypass of login/CAPTCHAs/browser blocks/paywalls (ERR_BLOCKED_BY_CLIENT stays a handoff item), no wider public-GitHub scope, clear public-API quota failures; offline regression fixtures (modal-covered curriculum, delayed menus, button-only resources, interrupted scans); launcher guide with one local command and honest limits. That repo is outside this project's working directories — the owner must open it (or grant access) before stage 4 can start.

## Course-material stage 2 — robust media ingestion (Claude, 2026-09-21, done)

`docs/slices/media-robust.md`. Delta: `knowledge/ingest/media.py` (RIFF probe, `normalize_wav` for every PCM/float depth, channel count and rate with size/format guards, model-aware transcript cache, `source_format` provenance), `vision.py` (bounded, versioned result cache keyed by image bytes + MIME + model + prompt version; hits carry zero usage), `loaders.py`/`service.py`/`runtime.py`/`core/config.py` (`vision_cache`, `--retranscribe` bypasses caches, cache hits logged as `cached=True` model calls), `tests/test_media_robust.py` (18), `usage.cost_report` counts cache hits apart from calls, `scipy` declared in the `stt` group. Code review applied (blank replies never cached, converter boundary kept, block-wise normalisation, LRU sweep). No full re-ingestion was run; the live corpus, `data/dev.db` and original media are untouched — the next `make ingest`/resume retries the 24-bit WAVs that failed before. **Owner:** add `VISION_CACHE_DIR=./data/vision` to `.env.example`; optionally run one real 24-bit lecture through Whisper and compare with its caption. Voice benchmark gate unchanged (open).

**Next: stage 3 — curate and draft one course.** Inspect the curriculum-draft flow (`kernel/curriculum.py`, `orchestrator/drafting.py`, Curriculum screen), add a reviewable source-selection step (primary teaching sources vs supplemental/community/bundled docs/decorative assets, originals and provenance retained), draft one owner-selected course (objectives, prerequisite skills, citations, learning objects, exercises, success criteria) with deterministic drafting or an explicitly selected local model, keep imported/searchable/draft/published states distinct, never publish or raise trust automatically; verify excluded sources do not influence the draft, citations resolve, duplicates do not inflate coverage, drafting does not alter learning progress; record open pedagogy/coverage questions.

## Course-material stage 1 — explain progress and failures (Claude, 2026-09-21, done)

`docs/slices/ingest-progress.md`. Delta from the inherited tree: `knowledge/ingest/service.py` (outcome classes + `classify`, `IngestProgress`/`should_stop`, `ingest_run`/`ingest_run_item` records, resume along the chain, links lists `reference_only`), `loaders.py` (marks links lists), `db/models.py` + migration `b7d1c0a5e2f3`, `api/corpus.py` + `schemas/corpus.py` (background jobs, runs list, outcome fields), `scripts/ingest.py` (progress lines, `--resume`, `--runs`), Makefile (`ingest … resume=1`, `ingest-runs`), Corpus screen (progress, outcome groups, resume). Tests: `tests/test_ingest_progress.py` (+ route tests), `Corpus.test.tsx`; `make migrate-check` ok; CLI smoke on a disposable DB. The live DB was not touched; nothing was ingested into it. Code review applied (see the slice doc: Ctrl-C as a clean stop, decoder/size/lockfile classification, resume retries `retryable_error` and keeps the run's settings, shutdown stops jobs, `knowledge/ingest/jobs.py`, deterministic stop→resume test, quiet live region, prompt pack git-ignored). Remaining limits in the slice doc (no progress inside one huge media file; a hard-killed run stays `running`; a resume skips by URI, changed files are caught by the next plain run).

## Current update — 2026-09-21: accepted decisions and local TTS

Owner accepted ADR-0011–0013 ("Paket annehmen"); statuses are now Accepted. Earlier pending-decision notes below are historical.

Kokoro is running in Docker as `audhs-kokoro`, bound only to `127.0.0.1:8880`, with restart policy `unless-stopped`. Image: `ghcr.io/remsky/kokoro-fastapi-cpu@sha256:ee3111d6a2c903ed62f3b4fa19543c6901205ed39fce3c177e895b34a8386b9c` (arm64). Local `.env` has `KOKORO_URL=http://127.0.0.1:8880`; restart the backend to reload settings. Stop/start: `docker stop audhs-kokoro` / `docker start audhs-kokoro`. Docker Desktop must be running.

Verified: GET `/v1/audio/voices` returned 72 voices; POST `/v1/audio/speech` with synthetic English text returned 101232 bytes PCM. No microphone capture or voice activation was performed. This is a service smoke test, not the Stage-5 latency/recognition benchmark; that gate remains open. Kokoro v1 does not provide German TTS.

Historical implementation order (completed in the four commits above):
1. Playwright journeys against isolated sandbox data: session boundaries/reload, review confidence, stop/resume; no paid providers.
2. CodeMirror Python editor: preserve drafts, keyboard escape, textarea fallback; browser regression coverage.
3. Encrypted backups: authenticated versioned format with bounded resource use, wrong-password/tampering/round-trip tests; retain learner default and explicit private full scope. Current ZIPs remain unencrypted.
4. Pinned locally served Pyodide plus NumPy/lock metadata: reproducible setup, ignored artifacts, offline tests, explicit missing-runtime error, no silent CDN fallback.

**Voice benchmark on LibriSpeech (2026-09-21, evidence — not the gate; corrected the same day).** The owner provided `dev-clean.tar.gz`; 50 utterances (two chapters, CC BY 4.0) were converted with `afconvert` to 16 kHz mono WAV in a scratch folder (nothing added to the repo) and `scripts/bench_voice.py` ran on a disposable DB with hosted providers off. The script now measures the stages separately (STT · LLM first token · LLM generation to the end · first sentence ready · pure TTS synthesis of that sentence · total to first audio with TTS started at the sentence boundary, as the real loop does) and projects a first-clause start; it takes `--transcripts` (WER), `--dataset` and `--own-recordings`, and without the latter labels the run as public-corpus evidence and never marks the Stage-5 gate. Result (`evals/results/bench_voice.json`, ROADMAP Stage 5): WER median 0 / mean 3.2 %; **first audio median 2.30 s, p95 2.94 s vs the 2 s target**; first-clause projection ≈ 1.76 s. The earlier claim that "TTS is 3.5 s of it" was wrong — that number had included the rest of the LLM generation; pure sentence synthesis is ≈ 1.0 s. **Next technical step (no own recordings needed):** let `voice/loop.py` start speaking at the first clause and measure it in a real run; then the owner's own utterances for the gate. The backend on :8000 was up all along.

**First-clause speech (2026-09-21, done).** `voice/loop.py`: `first_clause` + `speech_chunks` — the first utterance of a turn goes to TTS at the first closed clause (≥ 3 words, text after the comma), every later chunk stays a whole sentence; preference `voice.early_speech` (bool, default on) switches it off. `bench_voice.py --first-clause` measures the same decision: median **1.97 s** (p95 2.55 s) vs 2.45 s (p95 3.14 s) sentence-start on the same 25 LibriSpeech utterances — a 0.3–0.5 s gain (the baseline drifts ±0.15 s between runs); the median reaches the 2 s target, the p95 does not. The `spoke` event now records `first_chunk` so the gain is checkable in real sessions. Code review applied (interjections skipped, code spans/brackets/citations guarded, WS test through the loop); that test also caught a pre-existing whitespace bug in the sentence splitter ("Thenone"), fixed. Unit tests for the chunking; the WS tests still pass. Gate still open (own recordings).

Historical approval note: these four directions were subsequently implemented, reviewed and committed as recorded above. Use `/feature-slice` and its reviews for future changes. Keep the private implementation prompt pack untracked; do not push.

**Progress on the four slices (Claude, 2026-09-21):**
1. Browser journeys — **done** (`docs/slices/browser-journeys.md`): `frontend/playwright.config.ts` + `e2e/session-journeys.spec.ts` (block boundaries survive reload and a second tab; review confidence gates the reveal and the plan continues; stop here → resume from a fresh context at the next block), run by `make test-e2e` against `make dev-sandbox` (fresh `data/sandbox.db`, hosted providers off). Local: 7 journeys passed (session boundaries, review confidence, stop/resume, code editor, offline code run, missing runtime, wrong runtime version), repeatedly. Code review applied (see the slice doc). CI job `journeys` in `.github/workflows/ci.yml` **seen green remotely** on 2026-09-21 (run 35620554697). The Chromium build matching `@playwright/test` had to be installed into `~/Library/Caches/ms-playwright` (`pnpm exec playwright install chromium`, 94 MB).
2. CodeMirror editor — **done** (`docs/slices/code-editor.md`): `features/code/CodeEditor.tsx` (CodeMirror 6 with Python indentation, history, line numbers; Esc then Tab leaves; plain-textarea switch remembered per browser and used as the mount fallback), `e2e/code-editor.spec.ts` covers typing/indent/escape/draft/reset in Chromium. Code review applied (sticky Esc→Tab, fallback test). vitest 41, `make test-e2e` 4 passed. Open: syntax colouring needs `@codemirror/language` (not declared).
3. Encrypted backups — **done** (`docs/slices/encrypted-backups.md`): `backend/app/db/backup_crypto.py` (scrypt + AES-256-GCM chunked envelope, header bound by AAD, bounded memory, no stored password), `create_backup/inspect_backup/restore_backup` take a password, CLI `--encrypt`/`--password-file`, `make backup … encrypt=1`. Tests `test_backup_encrypted.py` (6: round trip, wrong password, flipped bytes, truncation/reorder/duplicate/trailing, crafted headers, plain paths) + CLI smoke on a disposable DB. Existing plain zips stay readable. **P5 bug found and fixed on the way:** the CLI restore refused every real backup (empty ORM metadata in a fresh process) — now imported explicitly and covered by a subprocess test; the backend tests now point `kokoro_url`/`ollama_host` at an unreachable port so the suite no longer depends on the owner's running services.
4. Local Pyodide — **done** (`docs/slices/local-pyodide.md`): `scripts/pyodide_runtime.py` / `make pyodide` installs the pinned 0.27.8 runtime + numpy into `frontend/public/pyodide/` (git-ignored) pinned by the committed `frontend/pyodide.manifest.json`; the worker loads only from `/pyodide/` and reports a missing runtime explicitly; CI caches the runtime by manifest hash. Code review applied (retry after a failed load, failure-path journeys, re-pin never trusts files on disk). Tests `test_pyodide_runtime.py` (4), `runner.test.ts`, three Playwright journeys in `code-run-offline.spec.ts` (offline run; missing runtime → explicit error, no external request; wrong version refused; the run one is skipped loudly without the runtime). **Owner:** `make pyodide` once on any machine that serves the app (16 MiB from jsDelivr, verified against the manifest afterwards) — done on this Mac on 2026-09-21.

**Historical commit grouping (completed locally; hashes above):** (a) `test(e2e): Playwright journeys on the sandbox stack` — frontend/playwright.config.ts, frontend/tsconfig.e2e.json, frontend/tsconfig.json, frontend/package.json (script), frontend/vite.config.ts, frontend/e2e/{helpers.ts,session-journeys.spec.ts}, Makefile (sandbox-*/test-e2e/lint), .claude/launch.json, .gitignore (test-results), .github/workflows/ci.yml (journeys job), .claude/rules/frontend.md, docs/slices/{browser-journeys,session-transitions}.md, CLAUDE.md; (b) `feat(code): CodeMirror editor with plain-textarea switch and fallback` — frontend/src/features/code/{CodeEditor.tsx,editorPreference.ts,CodeExercise.tsx,CodeExercise.test.tsx,CodeEditor.fallback.test.tsx}, frontend/src/test/setup.ts, frontend/e2e/code-editor.spec.ts, docs/slices/{code-editor,code-exercise}.md; (c) `feat(backup): password-based authenticated encryption envelope` — backend/app/db/{backup_crypto.py,backup.py}, scripts/backup.py, Makefile (backup targets), backend/tests/{test_backup_encrypted.py,conftest.py}, docs/{RECOVERY.md,slices/encrypted-backups.md,slices/backup-restore.md}; (d) `feat(code): pinned locally served Pyodide runtime` — scripts/pyodide_runtime.py, frontend/pyodide.manifest.json, frontend/src/features/code/pyodide.worker.js, backend/app/kernel/exercises.py, backend/tests/test_pyodide_runtime.py, frontend/e2e/code-run-offline.spec.ts, Makefile (pyodide), .gitignore (public/pyodide), .github/workflows/ci.yml (cache), CLAUDE.md, docs/slices/local-pyodide.md; then `docs(handoff)` with docs/HANDOFF.md + docs/IMPROVEMENT-PLAN.md. Never `frontend/public/pyodide/` (git-ignored, 16 MiB) and never the private prompt pack.

**Historical backend incident (currently healthy; no restart needed on recheck):** the `make dev` backend on :8000 was found dead on 2026-09-21 (uvicorn reloader alive, worker gone, every endpoint unreachable; touching a file did not respawn it). Claude is not allowed to stop the owner's process — please Ctrl-C that `make dev` and run it again so the backend picks up `KOKORO_URL`; then Models › Voice › verify should show the Kokoro server.

> **Latest continuation — Codex, 2026-09-20:** owner requested takeover. First P7 review fixes landed (clip/session validation; early Stop; audio cleanup), with passing tests/lint and scoped code/pedagogy reviews. **P7 full review remains pending.** Read `docs/CODEX-CONTINUATION.md` first for exact changes, verification and next actions. The earlier Claude history below is preserved; its “reviews pending” refers to the whole P7 implementation.

# Handoff — 2026-09-20 · P0–P8 done, P9 done with fakes (both reviewers, fixes applied) · voice benchmark + ADR pending (owner) · then: owner runs `/commit`

**Commits:** Stage 3 = `90dd8b8`, Stage 4 = `eb7626a`. **The improvement programme was committed on 2026-09-21 as 13 commits** (owner-invoked `/commit`, explicit file lists per group; shared files such as `db/models.py`, `orchestrator/tutor.py`, `routes/Session.tsx` and the generated `api-types.ts` sit in the group that owns most of their change, so only the final commit is guaranteed lint/test-green — `make lint` + `make test` were green on it: backend 245, frontend 39):
- `fd85f50` feat(ingest): every course-material format plus media transcription via the registry
- `70df040` docs(p0): improvement plan, reconciled skills and rules, sandbox dev target
- `1684d80` feat(p1): server-authoritative block transitions, review confidence, soft-timer identity, curriculum eligibility
- `b716c83` feat(p2): experiment analysis v2 and arm fidelity
- `b72b14e` feat(p3): vocab card identity and CSV import
- `a239a97` feat(p4): course to curriculum drafts, goal home, source viewer
- `3af8eee` feat(p5): backup/restore, CI gates, migration-upgrade test
- `d40aa9d` feat(p6): usage accounting, budget reservations, cost view
- `d6df60f` feat(p7): guided listening as the language block
- `a58fa40` feat(p8): Pyodide code exercise with deterministic checks
- `4abc4cd` feat(p9): voice setup, loop and language conversation (fakes; benchmark gate pending)
- `f43214f` chore(deps): declare CodeMirror python/commands and Playwright (ADR-0013)
- `5af2e33` docs(handoff): ADR 0011–0013, handoff with commit plan, shared agent notes

Still uncommitted on purpose: `.env.example` (the owner's change — the commit skill never stages `.env*`; commit it by hand with `git add .env.example && git commit -m 'chore(env): example voice and transcript settings'`) and `claude-code-implementation-prompts.md` (private prompt pack, never commit).

<!-- previous commit plan, kept for reference -->
**Commits:** Stage 3 = `90dd8b8`, Stage 4 = `eb7626a`. (historical) Everything since was uncommitted — the owner runs `/commit` per group, each with an explicit file list (never `git add -A`; `.env*`, `data/`, `evals/results/latest.json`, `backend/openapi.json` stay out; `claude-code-implementation-prompts.md` is the owner's private prompt pack — untracked, do not commit).** Suggested logical commits, in order: (1) `feat(ingest): every course format + media transcription via the registry` — backend/app/knowledge/ingest/**, models_ai registry bits (provider STT/VISION, routing_profiles, factory, registry, manage), api/schemas corpus, scripts/ingest.py, frontend corpus, ingest tests, `docs/slices/{ingest-formats,media-transcription}.md`, backend/uv.lock, pyproject `stt` group; (2) `docs(p0): improvement plan, reconciled skills/rules, migrate-check` — docs/IMPROVEMENT-PLAN.md, .claude/**, Makefile, CLAUDE.md, ROADMAP/ARCHITECTURE; (3) `feat(p1): server-authoritative block transitions, review confidence, soft-timer identity, curriculum eligibility` — kernel/{blocks,session,planner,skill_graph,practice,adaptation}.py, api/{plan,sessions}.py, alembic `5798bbccbea6`, frontend session/review/home/plan, slice docs, EVENT-SCHEMA; (4) `feat(p2): experiment analysis v2 + arm fidelity` — kernel/experiments.py, orchestrator/tutor.py arm fidelity, db/events.py keys, schemas/api experiments, Experiments.tsx, tests, `docs/slices/experiment-analysis-v2.md`; (5) `feat(p3): vocab identity + CSV import` — kernel/{practice,vocab_import,memory}.py, api/practice.py, alembic `ffb15bdebad6`, frontend practice/VocabImport, tests, slice docs; (6) `feat(p4): course → curriculum drafts, goal home, source viewer` — kernel/curriculum.py, orchestrator/drafting.py, prompts/curriculum, api/schemas curriculum, alembic `367349aa228b`, frontend curriculum/Home/Session, tests, slice docs; (7) `feat(p5): backup/restore, CI gates, migration-upgrade test` — db/backup.py, scripts/backup.py, Makefile targets, .github/workflows/ci.yml, tests/{_rows,test_backup_restore,test_migration_upgrade}.py, docs/RECOVERY.md, slice docs; (8) `feat(p6): usage accounting, budget reservations, cost view` — models_ai/{gateway,budget,usage,provider,ollama,claude,fake}.py, db/{traces,models}.py, alembic `ac95f48189c0`, api/deps + models_admin, frontend Models/Session, tests, slice doc; (9) `feat(p7): guided listening as the language block` — kernel/listening.py, orchestrator/listening.py, prompts/listening, api/schemas listening, alembic `e3ee9810831b`, grader domain stamp, frontend listening/*, tests, slice doc; (10) `feat(p8): Pyodide code exercise` — kernel/exercises.py, api/schemas exercises, grader kind code, frontend code/*, tests, slice doc; (11) `feat(p9): voice setup, loop and language conversation (fakes; benchmark gate pending)` — app/voice/**, api/voice.py, registry rows, prefs, prompts/voice, scripts/{spike_voice,bench_voice}.py, frontend voice/*, tests, slice docs. Run `make lint` and `make test` before each (`.claude/.tests-ran`). Added 2026-09-21 (fold into groups 2, 10 and 11): `make dev-sandbox` + the `dev-sandbox` launch config (Makefile, `.claude/launch.json`: fresh seeded `data/sandbox.db`, backend :8010, frontend :5174, `ANTHROPIC_API_KEY=` and `DAILY_BUDGET_USD=0`, no Qdrant reindex — for walk-throughs that must never touch `data/dev.db`; ports 8000/8001/5173 were taken by the owner's own servers); `frontend/vite.config.ts` proxy now `ws: true` (the voice WebSocket at `/api/voice/ws` did not pass through the Vite dev server before) and honours `API_PORT`; `orchestrator/grader.py` rubric next-step wording. `.env.example` got its three voice/transcript lines (`TRANSCRIPT_CACHE_DIR`, `STT_LANGUAGE`, `VOICE_DIR`) from the owner on 2026-09-21 — commit it with group 1/11.

**P1 (2026-09-20) — `docs/slices/{session-transitions,review-confidence,soft-timer-identity,curriculum-eligibility}.md`.** `kernel/blocks.py` owns block state in the (now single-row) session checkpoint: `start/end/advance/extend/end_running`, idempotent, one `block_started`/`block_ended` per block, event + checkpoint committed together, per-session `asyncio.Lock` in the routers, grasp evidence read from `attempted` events, `first_started_index` for honest plan strips, `block_type` persisted. API `GET /api/plan/blocks/state`, `POST /api/plan/blocks/{start,end,next,extend}`; `SessionOut.state` + `active_skill`; the checkpoint endpoint cannot move blocks (409). Frontend: `useBlockTransition` (one in-flight call), `routeForPhase`, StartCard / Continue card, honest "Which first?" labels, `SoftTimer` from server start time, Review sends `confidence_pre` per card and advances the plan, Home Resume routes by phase. `next_skill` skips non-teachable nodes (vocab decks; migration `5798bbccbea6`). Verified: `make test` backend 173 / frontend 22, `make lint` green, `make migrate-check` applied `5798bbccbea6` to a copy of the dev DB (and `make dev`'s auto-migrate applied it to `data/dev.db`), live walk-through in the browser pane (start card → movement → skip → review survives reload → continue → teach → resume "(teach)" → recap; DB shows one start/end per block, recap closed by `session_end`). Both reviewer agents ran; all blockers/majors fixed (see the slice docs). Playwright remains unapproved (decision recorded in IMPROVEMENT-PLAN).

**P2 (2026-09-20) — `docs/slices/experiment-analysis-v2.md`.** Analysis v2 in `kernel/experiments.py`: the assigned unit is the observation (unit means; events shown, never counted as n), Newcombe/Wilson for binary metrics, Welch t on unit means, no zero-width intervals, `MIN_UNITS_FOR_SUPPORT=10` before any direction, `OUTCOME_WINDOW_DAYS=30` after Stop, outcomes counted from the unit's assignment time, per-unit-type availability (node: no completion/continuation; session: no delayed recall/transfer), arm fidelity from `explained.context.{arm_intended, arm_delivered, arm_not_applied}` (written by `orchestrator/tutor.py` for node *and* session arms; Socratic = last sentence asks, ≤ 6 sentences, trailing `[n]` ignored; representation = detected kind matches; explicit arms are never failed for asking a check question), `MIN_FIDELITY_TURNS=2` before an exclusion, confounder + repeated-looking caveats, `analysis_version` on `measured`. `/experiments` shows the primary metric expanded, others collapsed, units + events + fidelity, unavailable metrics as one line. Both reviewers ran; blocker (explicit arm mis-judged) + majors fixed. **Not done by design:** the owner's real two-week run — protocol in the slice doc.

**P3 (2026-09-20, done) — `docs/slices/{vocab-identity,vocab-import}.md`.** Card identity = language + direction + NFC-normalised word + meaning (`practice.vocab_ref`; legacy refs adopted; migration `ffb15bdebad6` tested on a disposable DB); `kernel/vocab_import.py` CSV preview/import (quoting-aware delimiter sniff, header aliases incl. language codes, statuses `new|exists|reverse_only|duplicate_in_file|invalid`, bounded, idempotent, one transaction, `DeckIndex` for O(1) rows); `POST /api/vocab/import/preview`, `POST /api/vocab/import`; `/vocab` collapses "Add one card" and "Import a CSV list" (locked language/direction after preview, editable rows, undo remove, axe). Donor `lernapp-updates @ c2bf79f`. Both reviewers ran; 2 blockers + majors fixed (see the slice doc). **Owner decision:** PDF glossary import needs `pypdfium2` (not approved) — CSV until then.

**P4 (2026-09-20, done) — `docs/slices/{curriculum-drafts,goal-home,source-viewer}.md`.** `kernel/curriculum.py` (material status imported→searchable→draft→published; deterministic draft per course section; validation; publish with new learning-object versions; reject), `orchestrator/drafting.py` (optional local-model proposals, untrusted until reviewed), tables `curriculum_draft`/`content_report` + `skill_node.course` (migration `367349aa228b`), `/api/curriculum/*` (drafts, chunks for the citation viewer with a guarded open link, content reports), preference `goal.course` narrowing `next_skill`, `/curriculum` screen ("Lessons" in the nav), Home with goal selector + "next up" + duration preview + Resume first + "More options", `SourceViewer` + "Report this explanation" on the session screen. Tests: `test_curriculum.py` (15 incl. review regressions), `Curriculum.test.tsx`, `SourceViewer.test.tsx`, `Home.test.tsx`. Both reviewers ran; fixed: lecture order from file numbers (was alphabetical — blocker), section-scoped slugs, orphan/cycle validation over the published graph, typed draft payload (422 not 500), honest `origin` + note when the model route is unavailable, no fabricated MCQ provenance, versioned prompt `prompts/curriculum/draft.v1.md`, guarded file endpoint instead of dead `file://` links, goal never returns a locked node, Socratic not persisted + visible in the "More options" summary, content-word cloze with `guessable` flag, empty criteria/examples/exercises with validator warnings, `/curriculum` collapses to one open task. Remaining findings are listed per slice doc. **Acceptance walk (API-level):** ingest sample course → draft → fix assessments → publish → skills on the map → goal narrows `next_skill`; the tutor lesson → assessment → evidence → review path is the existing Stage-1 loop over the published nodes.

**P5 (2026-09-20, done; code-reviewed, 5 majors + minors fixed — see the slice doc) — `docs/slices/{backup-restore,ci-gates,migration-upgrade-test}.md`, `docs/RECOVERY.md`.** `db/backup.py` + `scripts/backup.py` (`make backup out=… [scope=learner|full] [transcripts=1]`, `make backup-inspect f=…`, `make backup-restore f=… target=…`): manifest v1 with sha256 per member, consistent SQLite snapshot (online-backup API), transcripts optional, secrets/models/vectors/originals excluded, Qdrant recorded as rebuildable (`reindex.py`); restore only into an empty target, closed member list, streamed extraction, symlink/traversal/size/version/revision checks, migrations inside the staging copy, rename into place, rollback on any failure. **Not encrypted** (`cryptography` not approved — owner decision). Default scope `learner` leaves purchased course text out (policy decision recorded; `full` prints a notice). CI `.github/workflows/ci.yml` (backend, frontend, migrations jobs; locked deps; no secrets) — first real run happens on the first push. `tests/test_migration_upgrade.py` upgrades Stage-0 and Stage-4 databases filled by reflection (`tests/_rows.py`) to head and checks the P1/P3 data steps. Donor `lernapp-updates @ 8b27582` (principles only). The live `data/dev.db` was never read or written during P5 (scratch databases only).

**P6 (2026-09-20, done; both reviewers, fixes applied — see the slice doc) — `docs/slices/usage-accounting.md`.** `model_call` gained `request_id`/`attempt`/`idempotency_key`/`outcome`/`usage_source`/`cost_status`/`reserved_usd` (migration `ac95f48189c0`; legacy rows labelled `legacy`), new `budget_reservation` table; `ModelGateway` reserves the worst case before every hosted attempt (per-process lock on `app.state.budget_lock`), logs one row per attempt (fallback, repair, blocked), reconciles in the same commit, does structured-output repairs itself (providers: one attempt, `MAX_REPAIRS=2`, then `invalid_output` + escalate), asks streams for the final usage chunk and labels estimates; failed hosted calls count at their reserved amount (`unknown`, never free). `GET /api/models/costs` + Models › Hosted spend card; routing rows say what is missing and the one step to fix it; the session screen says which model answered and why (fallback / budget / cut short). Tests `tests/test_usage_accounting.py` (11) incl. five concurrent calls against a small cap. **Not run:** live tutoring evals (need Ollama) — the only prompt change is the repair instruction. **Owner:** `ANTHROPIC_API_KEY` still empty, so every hosted route shows the actionable problem; nothing was spent.

**P7 (2026-09-20, done; both reviewers, 2 blockers + 5 majors + pedagogy findings fixed — see the slice doc) — `docs/slices/guided-listening.md`.** Guided listening as the language block, from ingested timed documents only (no lesson tables): `kernel/listening.py` (bounded clips with skip reasons, sibling media under the roots, per-language listening node, progress from attempts, source-cut cloze), `orchestrator/listening.py` (one model-proposed MCQ per clip, deterministic checks, labelled unvalidated), `/api/listening/*` (lessons, lesson, Range-capable media, task, `listened`), new verb `listened` (exposure only), grader events now carry the skill node's domain, preference `listening.document_id`, `LanguageBlock` chooser (vocab due / listening), `ListeningPanel` (play on click only, stop at clip end, replay, transcript reveal, reading version, confidence before feedback, next/stop with stop primary on low capacity), Language screen picker. Tests: `test_listening.py` (5, synthetic WAV + seed captions), `ListeningPanel.test.tsx` (2, axe). Not done: PyAV clip re-encoding (dependency not approved — bounds enforced on the element), MP3 duration checks, an upload path (ingest only).

**P8 (2026-09-20, done; both reviewers, 2 blockers + 4 majors + pedagogy findings fixed — see the slice doc) — `docs/slices/code-exercise.md`.** One source-linked code exercise (*Scaled dot-product attention in numpy*, node `attn-scaled`) as an `assessment` of kind `code`: `kernel/exercises.py` (catalogue, one row per node, public view without solution/hints, hint ladder, deterministic `grade_code` from client check results), `/api/exercises/*` (for-skill, hint, solution — logged as `explained`), submit through `/api/assess/attempt` (application evidence, card ≥ +2 days, hint/confidence recorded), `next_item` excludes it. Frontend `features/code/`: Pyodide `v0.27.8` from jsDelivr in a classic worker (network removed after load, MEMFS, 10 s terminate timeout, 20 k output cap), `CodeExercise` (Run ≠ Submit, stale-code rule, hints one per click, two-click full solution + check question, draft in localStorage, accessible loading/failure), offered collapsed on the session screen during teach/assess. Tests `test_exercises.py` (3), `CodeExercise.test.tsx` (3, fake runner). **Browser walk done 2026-09-21** (`make dev-sandbox`, see the slice doc): load, run, submit, check question, hints, solution, timeout and network lockdown all behaved; one wording bug in the rubric grader fixed. **Not done:** CodeMirror editor (dependency decision); more exercises.

**P9 (2026-09-20, done with fakes; both reviewers, 3 blockers + majors fixed — see the slice docs; benchmark gate + ADR pending) — `docs/slices/{voice-setup,voice-loop,language-voice-block}.md`.** `app/voice/` (VAD energy + optional Silero ONNX, MLX Whisper STT adapter, Kokoro server TTS client, setup lifecycle, WebSocket loop with cooperative interruption), registry rows `kokoro-82m` (server, never downloaded) + `silero-vad` (one ONNX file), `TaskClass.TTS`, preferences `voice.*`, `TurnRequest.conversation_lang` + `prompts/voice/conversation.v1.md`, `/api/voice/{readiness,verify,test-mic,activate,ws}`; frontend `features/voice/` (setup card on Models, Talk panel on the session screen, conversation option in the language block); `scripts/spike_voice.py` (all components pending on this Mac), `scripts/bench_voice.py` (refuses without the resources). Tests `test_voice.py` (7, fakes; Starlette TestClient WebSockets), `VoicePanel.test.tsx` (2), `VoiceSetup.test.tsx` (1). **Owner:** pull `whisper-large-v3-turbo`, `uv sync --group stt`, start a Kokoro server + `KOKORO_URL`, then Models › Voice: verify → test mic → activate; record ≥ 20 own utterances and run `scripts/bench_voice.py --wavs …` for the Stage-5 gate (**deferred by the owner on 2026-09-21: "continue without recordings for now" — the gate stays pending, nothing else is blocked on it**); run `/adr` for the voice stack (`TaskClass.TTS`, Kokoro server for TTS) before committing P9; note Kokoro-82M v1 has no German voice — DE conversation needs another TTS model. Nothing paid was used; hosted realtime not started.

**Ingest follow-up (2026-09-20, `docs/slices/{ingest-formats,media-transcription}.md`; code-reviewer blocker + 7 majors fixed, see the slice docs)**
- `knowledge/ingest/`: `transcripts.py` (SBV/ASS/TTML/JSON/TSV/timestamped text), `office.py` (docx/pptx/xlsx/odt/odp/ods — zip+XML, no new dependency), `epub.py`, `htmltext.py`, `rtf.py`, `latex.py`, `sourcecode.py` (secrets redacted), `structured_text.py`, `archives.py` (zip/tar, safety guards, `<archive>!/<member>` URIs, GitHub wrapper folder detection), `converters.py` (ffmpeg | afconvert, textutil, soffice, sips — discovered, never installed), `media.py` (`Transcriber` + `MlxWhisperTranscriber`, sidecar rule, cache), `vision.py` (`ImageReader` + `OllamaImageReader`), `runtime.py` (`transcriber_for`, `image_reader_for`, `capabilities`). `service.py`: `IngestOptions`, hash pre-check before decoding, `model_call` rows for `stt`/`vision`, archive expansion. Registry: `TaskClass.STT/VISION`, default `whisper-large-v3-turbo` (runtime `mlx`, role `stt`), routes `stt`/`vision`; `installed_models()` sees MLX snapshots; `bench` for stt. API `GET /api/corpus/capabilities`; `IngestRequest.language/media`. CLI `--language --no-media --capabilities`. `/corpus` shows readiness + all formats + skipped reasons. Optional dependency group `stt = [mlx-whisper]` (not installed).
- Real data: the whole `~/Downloads/Udemy Resources` folder parses (numbers in ROADMAP). The `agents-main.zip` contains ~7000 student notebooks under `community_contributions/` — ingest it deliberately (trust 1, or delete that folder from the zip first); the two course folders that are manifest-only are still empty.

**To make audio/video and images work (owner)**
1. `cd backend && uv sync --group stt` (installs `mlx-whisper`; MLX only for STT/TTS per ADR-0001).
2. `make models args="pull whisper-large-v3-turbo"` (≈1.6 GB HF snapshot into `data/models/mlx/…`), then `make models args="bench whisper-large-v3-turbo"`.
3. Optional `brew install ffmpeg` — without it macOS `afconvert` handles mp3/m4a/mp4/mov/wav/flac; ogg/opus/mkv/webm need ffmpeg.
4. Images: `make models args="pull gemma3-12b"` (multimodal; also fixes the Socratic arm). `make ingest src=… ` then reports `N media transcribed`. `uv run python scripts/ingest.py --capabilities` shows the current state.

**Done this session** (repo `~/projects/audhs-tutor`, branch `main`)
- Stage 4, all four slices (`docs/slices/{adaptation-proposals,adaptive-ux-bundle,experiments,domain-blocks}.md`):
  - `kernel/adaptation.py` — five observed-pattern rules → cards (Try / Make default / No / Don't suggest again), trials expire at the next session, undo, log; `/api/adaptations`; `AdaptationCards` on Home + Session, `AdaptationLog` under Preferences. Migration `064e2993cba7`.
  - Bundle — `planner.replan` + `/api/plan/replan` (energy check-in → planner card, `pref="session.plan"`), `/together` presence screen (opt-in brown noise), sensory prefs (`ui.theme/font_scale/notifications/ambient`) applied by `useSensory`, `models_ai/manage.py` + `/api/models` (+ background pull/bench jobs) + `/models` screen (the CLI delegates to `manage`), parking promote/drop + Home reminders, wind-down notification behind a preference.
  - `kernel/experiments.py` — templates, balanced matched-node / session assignment (logged `assigned`, arm stamped on every event and the turn meta; node-unit arm sets the questioning style per node in `TutorTurn`; session-unit arm sets `session.socratic`), outcomes from the event log (delayed recall counts only reviews ≥ 1 day after the previous review), results with 95 % CI and a literal reading; `/api/experiments`; `/experiments` dashboard; session banner. Migration `e2adb21ba4ae` (`experiment_assignment`, `session.experiment_arm_id`).
  - `kernel/practice.py` — vocabulary decks on FSRS in the LANGUAGE domain (only ever in the language block; `memory.due_items(domain=…, exclude_domains=…)`), `practiced` events, `/api/practice`, `/api/vocab`, `PracticePanel` (movement/guitar: 2–3 options, rating, skip) + `VocabPanel`, `/vocab`; the session now runs movement primers and domain-switch blocks instead of skipping them.
  - Fix found by the benchmark: `memory.review` now measures `days_since_learned` from the previous review (was wall-clock item creation → negative under time travel).

**Verified by**
- `make test`: backend 119 passed, frontend 18 passed. `make lint` green. (Occasionally a `libc++abi … recursive_mutex lock failed` line prints *after* all tests passed — an onnxruntime/thread teardown message at interpreter exit, seen in some full runs only; not a test failure. If it bothers CI, run `pytest -p no:cacheprovider` in two halves or investigate the fastembed teardown.)
- `make bench s=4`: 5/5 (numbers in ROADMAP). The two-week experiment is *simulated* with time travel; the owner's real run is the actual benchmark: create "Socratic vs explicit" under `/experiments`, Start, learn normally for two weeks, then read Results.
- Browser pane: `/models` (real registry, reranker 81.6 ms / 16 docs), `/together`, `/experiments` (empty state), Home unchanged.
- Kit reviews (second attempt; the first hit the monthly spend limit): pedagogy-reviewer "ship-with-fixes" → all 9 fixes applied; code-reviewer 3 blockers (Try without a session became permanent; trials expired *after* the next plan was built; the short-vs-long template changed nothing) + 7 majors → all applied, plus most minors. Regression tests added for each blocker/major. Backend 122 tests, frontend 18, lint green, bench 5/5 after the fixes.

**Open / owner decisions**
- Run the real experiment (above). While it runs, every explain turn on an assigned node uses that node's arm; the session screen says so.
- `gemma3:12b` still not pulled (Socratic arm on llama3.1:8b tends to explain instead of asking — the experiment will show it). `ANTHROPIC_API_KEY` still empty (`grade_rubric`, `judge` unresolved in the routing table).
- Guitar activities are a fixed list; vocab has no CSV import. Ingest: `.ppt/.key/.pages` need LibreOffice or an export; Whisper quality on the German/English lectures is unmeasured until the model is pulled.
- Stage 5 (`escalation-polish`, `voice-loop`, `language-voice-block`) needs the API key and the MLX voice stack (`scripts/spike_voice.py` may be run first).

**Test harness notes (2026-09-20)**
- `FakeProvider.hash_vector` now uses crc32: Python's salted `hash()` made `test_ingest_formats::test_mixed_folder_end_to_end` (and any retrieval-ranking assertion on the fake embedder) flaky across processes.
- WebSocket tests (`tests/test_voice.py`) use Starlette's `TestClient`; the app creates its own engine in its lifespan (aiosqlite connections are loop-bound) and builds the retrieval repo via `app.state.repo_factory` inside the app thread (sqlite3 handles are thread-bound). Never `task.cancel()` a coroutine that may sit in an aiosqlite call — the voice loop interrupts cooperatively for that reason. macOS has no `timeout`; use a Python `subprocess.run(..., timeout=)` when a suite might hang.

**Review leftovers (deliberately not done)**
- Out-of-session events (parking promote, standalone practice, session-less cards) stamp `mode=steady, energy=3` as a sentinel — use the latest open session if you need cleaner cuts.
- `started` is emitted after the arm is assigned now, so it carries the arm; older sessions (before today) do not.
- Node-unit experiments cannot measure `completion`/`transfer` (block and evidence events are not per node) — those metrics read "not enough data" for node-unit experiments.
- `parking drop` writes no event (the verb enum is closed; add `dropped` via EVENT-SCHEMA if wanted).
- Vocab review has no confidence step (FSRS rating, not graded feedback).
- Route/kernel split: `plan.replan` and `practice.add` still do a little assembly in the router.
- Frontend tests for the energy check-in UI, `useSensory` and `SoftTimer notify` are missing; new route tests do not run axe.

**Do not**
- Assume a card was applied silently: everything the system changes is in Preferences › Adaptation log with Undo; if a preference looks changed, look there first.
- Mix domains in a block: `due_items` excludes `language` by default; pass `domain="language"` for the vocab block.
- Start two experiments of the same unit type (the API refuses).
- Run `make dev` while :8000/:5173 are taken (dev servers from today are still running).
- Hand-edit `api-types.ts`; `make gen-api` after schema changes. Exact-string edits after ruff/prettier: assert the match count.
